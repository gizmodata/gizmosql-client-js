{
  "name": "@gizmodata/gizmosql-client",
  "version": "2.0.0",
  "description": "A TypeScript/JavaScript client for GizmoSQL and Apache Arrow Flight SQL",
  "author": "GizmoData LLC",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gizmodata/gizmosql-client-js.git"
  },
  "homepage": "https://github.com/gizmodata/gizmosql-client-js#readme",
  "bugs": {
    "url": "https://github.com/gizmodata/gizmosql-client-js/issues"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/",
    "scripts/",
    "driver-manifest.json",
    "README.md",
    "LICENSE",
    "NOTICE"
  ],
  "scripts": {
    "prebuild": "npm run clean",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist/",
    "postinstall": "node scripts/download-driver.cjs",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "jest --config jest.unit.config.js --testPathIgnorePatterns=integration",
    "test:unit": "jest --config jest.unit.config.js --testPathIgnorePatterns=integration",
    "test:integration": "NODE_OPTIONS=--experimental-vm-modules jest --testPathPatterns=integration",
    "test:all": "npm run test:unit && npm run test:integration",
    "test:watch": "jest --config jest.unit.config.js --watch --testPathIgnorePatterns=integration",
    "lint": "eslint src test --fix",
    "lint:ci": "eslint src test",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build && npm test && npm run lint:ci"
  },
  "keywords": [
    "gizmosql",
    "gizmodata",
    "arrow",
    "flight",
    "flightsql",
    "apache-arrow",
    "grpc",
    "sql",
    "database",
    "analytics",
    "duckdb"
  ],
  "dependencies": {
    "@apache-arrow/adbc-driver-manager": "^0.24.0",
    "apache-arrow": "^21.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@types/google-protobuf": "^3.15.12",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.3.3",
    "eslint": "^9.35.0",
    "eslint-plugin-jest": "^29.0.1",
    "eslint-plugin-unicorn": "^61.0.2",
    "grpc_tools_node_protoc_ts": "^5.3.3",
    "grpc-tools": "^1.13.1",
    "jest": "^30.1.3",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.4.1",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.43.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "overrides": {
    "test-exclude": "^7.0.1"
  }
}
